<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.gms.common.SignInButton
        android:id="@+id/sign_in_button"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"/>

    <Button
        android:id="@+id/sign_out_button"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:text="Sign Out"
        android:layout_below="@+id/sign_in_button"
        android:background="@drawable/sign_out_button"
        />

    <Button
        android:id="@+id/sign_out_and_disconnect_button"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:text="Sign Out and Disconnect Account"
        android:layout_below="@+id/sign_out_button"
        android:background="@drawable/sign_out_button"
        />


    <TextView
        android:id="@+id/sign_in_page_text"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Or, create your own username and password:"
        android:layout_below="@+id/sign_out_and_disconnect_button"
        />

    <EditText android:id="@+id/username"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Username"
        android:layout_below="@+id/sign_in_page_text"
        />

    <EditText android:id="@+id/password"
        android:inputType="textPassword"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:layout_below="@+id/username"
        />

    <Button
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:id="@+id/submit_button"
        android:text="Submit"
        android:layout_below="@+id/password"
        />
